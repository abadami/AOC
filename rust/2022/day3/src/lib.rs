use std::collections;

pub fn part1(input: &String) -> i64 {
  let scenarios: Vec<&str> = input.split("\r\n").collect();

  scenarios.iter();

  return 0;
}

pub fn part2(input: &String) -> i64 {
  let scenarios: Vec<&str> = input.split("\r\n").collect();

  return 0;
}