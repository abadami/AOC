pub fn part1(input: &String) -> i64 {
  let split_values: Vec<&str> = input.split("\n").collect();

  return 0;
}

pub fn part2(input: &String) -> i64 {
  let split_values: Vec<&str> = input.split("\n").collect();

  return 0;
}